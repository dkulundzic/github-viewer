import XCTest
@testable import GithubViewerUserInterface

final class GithubViewerUserInterfaceTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GithubViewerUserInterface().text, "Hello, World!")
    }
}
